# ══════════════════════════════════════════════════════════════════
#  متطلبات منظومة العمل الخيري الذكي — Smart Charity Ecosystem
#  جميع المكتبات مجانية ومفتوحة المصدر
# ══════════════════════════════════════════════════════════════════

# ── الخادم الخلفي (Backend) ─────────────────────────────────────
fastapi>=0.109.0              # إطار API سريع وحديث
uvicorn[standard]>=0.27.0     # خادم ASGI عالي الأداء
pydantic>=2.5.0               # التحقق من البيانات وتوثيقها
python-dotenv>=1.0.0          # تحميل إعدادات .env
python-jose[cryptography]>=3.3.0  # JWT authentication
supabase>=2.0.0               # قاعدة بيانات Supabase (اختياري — يتراجع لـ JSON)

# ── تصدير البيانات ────────────────────────────────────────────────
openpyxl>=3.1.0               # إنشاء ملفات Excel (.xlsx)
xlsxwriter>=3.1.0             # كتابة Excel متقدمة (بديل)

# ── قاعدة البيانات ────────────────────────────────────────────────
# SQLite: مدمجة في Python (لا تحتاج تثبيت)
# PostgreSQL (اختياري للإنتاج):
# psycopg2-binary>=2.9.0
# sqlalchemy>=2.0.0

# ── المكتبات القياسية (لا تحتاج تثبيت) ──────────────────────────
# json, csv, logging, datetime, uuid, hashlib, dataclasses
# typing, os, sys, math, re, pathlib

# ── التطوير والاختبار ─────────────────────────────────────────────
# pytest>=8.0.0              # تشغيل الاختبارات
# httpx>=0.26.0              # اختبار FastAPI endpoints
