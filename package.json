{
  "name": "smart-charity-ecosystem",
  "version": "1.0.0",
  "description": "Backend starter via npm for Render.com",
  "scripts": {
    "start": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1",
    "build": "pip install -r requirements.txt"
  }
}
